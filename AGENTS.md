# 문서 저장소 작업 규칙

## 목적

이 저장소는 스킬, 하네스, 개발 루프, 파이프라인, 프로젝트 스캐폴딩을 문서화하고 재사용하기 위한 저장소다.

## 범위

- 현재 저장소는 문서와 프로젝트 스캐폴딩 위저드만 관리한다.
- 실제 애플리케이션 코드는 추가하지 않는다.
- 스캐폴딩 위저드는 선택 결과와 생성 계획을 만들고, 명시적으로 요청된 경우에만 외부 공식 생성기를 실행한다.
- 임의의 원격 스크립트를 자동으로 다운로드하거나 실행하지 않는다.
- 모든 내용은 하나의 Git 저장소에서 관리한다.
- 프로젝트별 구현은 `docs/compositions/`의 명세로만 표현한다.

## 문서 규칙

- 문서 하나는 하나의 책임만 갖는다.
- 공통 규칙과 계층별 규칙을 분리한다.
- 결정되지 않은 내용은 추측하지 않고 `미결정`으로 표시한다.
- 선택 사항은 반드시 `선택 사항`이라고 표시한다.
- 경로, 파일명, 명령어는 코드 형식으로 표기한다.
- 기존 용어와 다른 의미로 사용하는 단어는 `docs/model/glossary.md`에 먼저 정의한다.

## 변경 전 확인

1. 관련 문서를 먼저 찾는다.
2. 기존 규칙과 중복되는지 확인한다.
3. 한 문서에 넣을 내용인지 별도 문서가 필요한지 판단한다.
4. 실제 실행물 추가가 필요한 경우 먼저 범위를 확인한다.
