# Monorepo: web + backend + mobile

```text
app/web      목적에 따라 Next.js 또는 React + Vite
app/backend  백엔드 스택
app/mobile   React Native + Expo + TypeScript
```

모바일에는 웹용 Tailwind 또는 vanilla-extract를 강제하지 않는다. 상태·데이터·검증·폼 도구는 기능이 필요할 때만 선택한다.
