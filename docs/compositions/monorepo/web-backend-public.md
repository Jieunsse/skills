# Monorepo: public web + backend

```text
app/web      Next.js + TypeScript + Tailwind CSS
app/backend  백엔드 스택
```

SEO가 중요하지 않은 관리자·내부 도구라면 `app/web`을 React + Vite로 선택한다. 루트 `docs/`는 전체 시스템을, 각 계층의 `docs/`는 계층별 내용을 관리한다.
